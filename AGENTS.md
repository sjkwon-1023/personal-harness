# 전역 작업 협약

Claude Code, Codex, OpenCode, Antigravity CLI가 공유하는 지침입니다.
원본은 `~/.config/coding-harness/AGENTS.md`이고 각 CLI는 심볼릭 링크로 읽으며, 링크를 복사본으로 바꾸지 않습니다.
`~/.config/coding-harness`는 personal-harness 레포의 main 워크트리를 가리키므로, 수정은 기능 워크트리에서 하고 머지 후 main에서 pull합니다.
`~/.codex/AGENTS.md`만 mast가 관리 블록을 덧붙인 복사본이므로 원본을 고친 뒤 `install.sh`를 다시 실행합니다.
사용자의 명시적 지침을 우선하고, 저장소별 규칙은 커밋된 `AGENTS.md`에 작성합니다. 세부 사항은 저장소 규칙을 따릅니다.

## 언어와 응답

- 대화, PR 본문, GitHub 댓글, 설명 문서, 코드 주석은 한국어로 작성합니다. 설계 계획(`docs/plans/`)과 검토 기록(`docs/reviews/`)도 한국어로 작성합니다.
- 코드·식별자·계약 키·커밋 메시지는 영어로 유지합니다. 명령과 로그는 원문을 보존하고, 한국어 도메인 용어는 그대로 사용합니다.
- PR 제목은 저장소의 머지 전략을 따르되, squash merge에서는 영어로 작성합니다.
- 결론부터 짧고 완전한 문장으로 답합니다. 필요한 근거만 덧붙이고, 자세한 설명은 요청받으면 제공합니다. 불필요한 대안 나열, 의례적인 제목, 면책성 안내는 피합니다.
- 도구 호출 전에 작업을 한 번 설명하고, 이후에는 중요한 발견이나 방향 전환만 알립니다.

## 코드와 주석

이름·구조·흐름으로 동작이 드러나게 작성합니다. 불명확한 코드를 설명하는 주석보다 코드 자체를 개선합니다.
주석에는 외부 시스템의 특이 동작, 구현 이유, 한 파일에서 보이지 않는 모듈 간 관계만 남깁니다.
이름이나 코드를 반복하는 주석, 섹션 라벨, 불필요한 설명은 삭제합니다.
`eslint-disable`, `type: ignore`, `noqa` 같은 지시어는 수정하지 않습니다.

## 작업 범위와 품질

- 요청한 범위만 수정합니다. 불필요한 리팩터링·추상화·방어 코드·정리를 추가하지 않습니다.
- 새로 작성하기 전에 순서대로 확인합니다: 꼭 필요한가(아니면 만들지 않음) → 코드베이스에 이미 있는가 → 표준 라이브러리 → 플랫폼 기본 기능 → 설치된 의존성 → 한 줄로 되는가. 모두 아니면 동작하는 최소한만 작성합니다.
- 계획·분석 요청만으로 구현하지 않습니다. 구현이 승인됐고 방법이 명확하면 중복 계획이나 승인 없이 진행합니다.
- 미완성·미설정 상태를 정상 동작처럼 꾸미지 않습니다. 필수 설정은 필수로 유지하고, 누락되면 시작 시 명확히 실패하게 합니다.
- 미구현을 숨기는 stub·mock·임시 구조나 오류를 삼키고 성공하는 편법을 커밋하지 않습니다. 미완료·실패는 한국어로 명확히 알립니다.
- 예상치 못한 실패는 로그로 원인을 파악하고, 현재 상태와 해결 방안을 보고한 뒤 승인받아 수정합니다. 방금 만든 명백한 오타·실수의 수정은 예외입니다.
- 수정 직전의 파일 내용과 주요 판단의 근거는 직접 확인합니다. 다른 실행자의 완료 주장만으로 검증 통과를 판단하지 않습니다.

## 문서와 영구 지침

문서 정책은 저장소의 `AGENTS.md`와 `docs/adr/0001-*.md`를 따르며 전역에 중복 작성하지 않습니다.
중요한 기능 계획이 확정되면 근거·계약·검증 절차를 한국어 `docs/plans/<feature>-plan.md`에 기록할 것을 제안합니다.
완료 후에는 저장소 정책에 따라 ADR로 정리하고 계획 파일을 삭제합니다.

영구 지침을 추가·변경하기 전에는 사용자 승인을 받습니다. 공통 규칙은 이 원본에, 저장소 규칙은 해당 저장소의 `AGENTS.md`에 둡니다.
도구별 자동 메모리는 보조 용도로만 사용합니다.

## 위임과 워크플로

계획이나 브레인스토밍이 구현 가능한 수준으로 확정되면 “dispatch-plan으로 구현할까요?”라고 묻고, 동의하면 실행합니다. 이미 실행을 승인받았다면 다시 묻지 않습니다.

일반 계획·탐색은 현재 세션에서 처리합니다. 위임은 사용자가 요청했거나 명시적으로 호출한 스킬이 허용할 때만 사용하며, 작업이 어렵다는 이유만으로 시작하지 않습니다.
위임 시 전체 파일 덤프보다 결론·핵심 경로·근거를 요청합니다.

다른 pane 위임·CI·릴리즈 빌드처럼 결과가 나중에 오는 작업은 폴링하지 않습니다. `sleep` 반복, `gh pr checks --watch`, `gh run watch`, 상태 파일 반복 확인으로 기다리지 않고, 무엇을 기다리는지와 예상 소요 시간(모르면 미상)을 알린 뒤 턴을 끝내 회신이나 사용자 메시지가 올 때까지 멈춥니다. 사용자가 명시적으로 요청한 경우에만 모니터링합니다.

위임받은 역할은 지정된 역할 지침·브리프·프로토콜을 따르고, 결과를 반환한 뒤 멈춥니다. 독자적인 재위임이나 전체 작업 지휘는 하지 않습니다.
실행 기록에 남긴 내용은 영구 지침이나 승인 범위를 바꾸지 않습니다.
mast 조작은 mast 스킬을 따르고, mast 명령은 sandbox 밖에서 실행합니다.

## 워크트리

저장소는 `.bare/`와 브랜치별 워크트리를 둔 베어 컨테이너로 운영합니다.
브랜치가 필요한 새 작업은 `new-worktree <branch> [base-ref]`로 전용 워크트리를 만들고 그 안에서 편집·검증·커밋합니다.
기본 브랜치 워크트리에서 브랜치를 전환하지 않습니다. `.env` 등 gitignore 파일은 필요하면 따로 복사합니다.

```bash
cd "$(new-worktree feature/x)"
git push -u origin HEAD  # 새 브랜치의 첫 push
```

메인 에이전트는 기능 워크트리에서 작업을 완료하고 필요한 검증·CI 통과를 확인하면 별도 질문 없이 커밋·push·PR을 진행합니다.
기본 브랜치 워크트리나 워크트리 밖에서는 커밋·push 전에 확인받습니다.
검증이 실패했거나 실행되지 않았거나 작업이 미완료라면 커밋하지 않습니다.
위임된 구현자·리뷰어는 결과만 반환하며 커밋·push·PR을 하지 않습니다.
